services:
  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.17.3
    container_name: elasticsearch
    environment:
      - discovery.type=single-node
      - ES_JAVA_OPTS=-Xms512m -Xmx2g # Limit memory usage
      - "ELASTICSEARCH_USER=${ELASTICSEARCH_USER}"
      - "ELASTICSEARCH_PASSWORD=${ELASTICSEARCH_PASSWORD}"
      # Credentials for local testing:
      # - "ELASTICSEARCH_USER=elastic"
      # - "ELASTICSEARCH_PASSWORD=changeme"
      - xpack.security.enabled=false # Disable security for now
      - xpack.security.transport.ssl.enabled=false
      - "xpack.ml.enabled=false" # Disable machine learning, to save memory
    ports:
      - "30051:9200"
      # Ports for local development
      # - "9200:9200"
    ulimits:
      memlock: -1
    deploy:
      resources:
        limits:
          memory: 2GB # Limit memory usage
    volumes:
      - esdata:/usr/share/elasticsearch/data
    networks:
      - project_network

  kibana:
    image: docker.elastic.co/kibana/kibana:8.17.3
    container_name: kibana
    depends_on:
      - elasticsearch
    ports:
      - "30052:5601"
      # Ports for local development
      # - "5601:5601"
    networks:
      - project_network

  logstash:
    image: docker.elastic.co/logstash/logstash:8.17.3
    container_name: logstash
    environment:
      - LOGSTASH_INTERNAL_PASSWORD=${LOGSTASH_INTERNAL_PASSWORD}
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
    depends_on:
      - elasticsearch
    volumes:
      - ./config/logstash.conf:/usr/share/logstash/pipeline/logstash.conf
    ports:
      - "30053:5044" # Beats input
      - "30054:5000/tcp" # Logstash TCP input
      - "30054:5000/udp" # Logstash UDP input
      # Ports for local development
      # - "5044:5044" # Beats input
      # - "5000:5000/tcp" # Logstash TCP input
      # - "5000:5000/udp" # Logstash UDP input
    networks:
      - project_network
  #RabbitMQ Instance for local development, comment out for deployment on VM
  # rabbitmq:
  #   image: rabbitmq:3-management
  #   container_name: rabbitmq
  #   ports:
  #     - "5672:5672"
  #     - "15672:15672"
  #   networks:
  #     - elk

  consumer: #Will receive messages from RabbitMQ and pass them to Logstash
    container_name: consumer
    environment:
      - RABBITMQ_USER=${RABBITMQ_USER}
      - RABBITMQ_PASS=${RABBITMQ_PASS}
    depends_on:
      - elasticsearch
      - logstash
    build: ./consumer
    networks:
      - project_network

  publisher: #Will send test messages to RabbitMQ, replicating real services
    container_name: publisher
    environment:
      - RABBITMQ_USER=${RABBITMQ_USER}
      - RABBITMQ_PASS=${RABBITMQ_PASS}
    depends_on:
      - elasticsearch
    build: ./publisher
    networks:
      - project_network

volumes:
  esdata:

networks:
  project_network:
    driver: bridge
